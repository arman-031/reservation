# Reservation

A Django reservation backend currently under development.

## Status

This project is actively being developed. The API, reservation workflow, validation, authentication, and production configuration are not complete yet.

## Development

The project uses Django with the `turns` application. More documentation will be added as the reservation domain and API stabilize.
